<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.twtstudio.retrox.schedule.view.ToScheduleActViewModel" />
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp"
        android:layout_height="80dp"
        android:clickable="true"
        bind:clickCommand="@{viewModel.clickCmd}"
        android:foreground="?android:attr/selectableItemBackground"
        bind:cardElevation="2dp"
        bind:cardCornerRadius="2dp"
        bind:cardBackgroundColor="@color/schedule_green3">

        <TextView
            android:layout_gravity="center"
            android:textColor="@color/white_color"
            android:text="进入课程表详情页"
            android:textSize="20sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </android.support.v7.widget.CardView>
</layout>
