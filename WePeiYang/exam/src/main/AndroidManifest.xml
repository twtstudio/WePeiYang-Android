<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twtstudio.service.tjwhm.exam">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".home.ExamHomeActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".list.ListActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".problem.ProblemActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".problem.score.ScoreActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".user.history.HistoryActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".user.star.StarActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>
