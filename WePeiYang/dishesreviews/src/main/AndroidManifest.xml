<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twtstudio.service.dishesreviews" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DishesReviewsTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".search.view.SearchActivity"/>
        <activity android:name=".dish.view.DishActivity" />
        <activity android:name=".canteen.CanteenActivity" />
        <activity android:name=".share.ShareActivity" />
        <activity android:name=".evaluate.EvaluateActivity" />
    </application>

</manifest>